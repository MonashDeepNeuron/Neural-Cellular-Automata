# Motorola Project

Goal: To train an NCA to build bridges between artworks.
We want the NCA to highlight similar artworks
(e.g. via thicker, shorter bridges).
The core idea is to create a novel way to 

Milestones:
1. A simplified version of the problem: the NCA can bridge between colours,
with similar colours having thicker bridges.

Setup:
Run `pip install numpy matplotlib Pillow`.
Run `pip install torch torchvision torchaudio`
(if using an AMD GPU,
append `--index-url https://download.pytorch.org/whl/rocm6.3`).
