version: 1
frontend:
    phases:
        preBuild:
            commands:
                - cd web
                - pnpm install
        build:
            commands:
                - pnpm build
    artifacts:
        baseDirectory: web/.next
        files:
            - '**/*'
    cache:
        paths:
            - node_modules/**/*